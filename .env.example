# Backend
# DATA_DIR=./data
# DATA_DIR=./data
DATA_DIR=D:\vh-smoke-data
LOG_LEVEL=info
YTDLP_COOKIES_FILE=
#LLM setting:optional,recommend to set in the web or desktop,which sqlite and its proivity is higher than setting in the .env file
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=
LLM_MAX_ATTEMPTS=3
LLM_TIMEOUT_S=60
# Enable background worker in local dev so jobs actually run.
WORKER_ENABLE=1

